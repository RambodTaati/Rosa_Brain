{"skill": "web_languages_exam", "question": "A form posts profile updates with CSRF disabled and reflects the bio field into HTML via innerHTML. Name two distinct defenses.", "answer": "Add CSRF tokens (or SameSite cookie strategy) for state-changing posts, and render the bio with textContent/escaping instead of unsanitized innerHTML.", "text": "Q: A form posts profile updates with CSRF disabled and reflects the bio field into HTML via innerHTML. Name two distinct defenses. A: Add CSRF tokens (or SameSite cookie strategy) for state-changing posts, and render the bio with textContent/escaping instead of unsanitized innerHTML."}
{"skill": "web_languages_exam", "question": "You need a responsive card gallery without hard breakpoints for column count. Which CSS grid technique fits?", "answer": "Use repeat(auto-fit, minmax(minTrack, 1fr)) so columns wrap by available width.", "text": "Q: You need a responsive card gallery without hard breakpoints for column count. Which CSS grid technique fits? A: Use repeat(auto-fit, minmax(minTrack, 1fr)) so columns wrap by available width."}
{"skill": "web_languages_exam", "question": "A React list uses index keys and users can reorder items. What bug appears and how do you fix it?", "answer": "State can attach to the wrong row after reorder; use stable unique ids as keys.", "text": "Q: A React list uses index keys and users can reorder items. What bug appears and how do you fix it? A: State can attach to the wrong row after reorder; use stable unique ids as keys."}
{"skill": "web_languages_exam", "question": "fetch('/api') returns HTTP 503. Why might a naive try/catch around await fetch miss it?", "answer": "fetch only rejects on network failure; 503 still resolves, so you must check res.ok/status.", "text": "Q: fetch('/api') returns HTTP 503. Why might a naive try/catch around await fetch miss it? A: fetch only rejects on network failure; 503 still resolves, so you must check res.ok/status."}
{"skill": "web_languages_exam", "question": "TypeScript function accepts unknown JSON. What is the safe first step before reading fields?", "answer": "Narrow/validate the shape (type guards or schema) before property access; do not cast blindly to a rich interface.", "text": "Q: TypeScript function accepts unknown JSON. What is the safe first step before reading fields? A: Narrow/validate the shape (type guards or schema) before property access; do not cast blindly to a rich interface."}
{"skill": "web_languages_exam", "question": "PostgreSQL query builds WHERE email = '{user}' via f-string. What is the correct rewrite pattern conceptually?", "answer": "Use a bound parameter placeholder and pass the email separately to the driver/execute.", "text": "Q: PostgreSQL query builds WHERE email = '{user}' via f-string. What is the correct rewrite pattern conceptually? A: Use a bound parameter placeholder and pass the email separately to the driver/execute."}
{"skill": "web_languages_exam", "question": "PHP prints $_GET['q'] into a results heading. What exact risk and primary fix?", "answer": "Reflected XSS risk; escape with htmlspecialchars (ENT_QUOTES, UTF-8) or a templating auto-escape.", "text": "Q: PHP prints $_GET['q'] into a results heading. What exact risk and primary fix? A: Reflected XSS risk; escape with htmlspecialchars (ENT_QUOTES, UTF-8) or a templating auto-escape."}
{"skill": "web_languages_exam", "question": "A sticky header covers focused links when tabbing. Which accessibility-oriented CSS idea helps?", "answer": "Ensure focus visibility and account for scroll-padding/offset so focused targets are not hidden under sticky UI.", "text": "Q: A sticky header covers focused links when tabbing. Which accessibility-oriented CSS idea helps? A: Ensure focus visibility and account for scroll-padding/offset so focused targets are not hidden under sticky UI."}
{"skill": "web_languages_exam", "question": "Node serves files from /var/data using path.join(root, req.path). User sends ../../etc/passwd. Defense?", "answer": "Resolve the path and verify it remains under the trusted root before reading.", "text": "Q: Node serves files from /var/data using path.join(root, req.path). User sends ../../etc/passwd. Defense? A: Resolve the path and verify it remains under the trusted root before reading."}
{"skill": "web_languages_exam", "question": "Vue app uses v-html with forum posts. What is the correct policy?", "answer": "Do not v-html untrusted HTML; sanitize with a vetted library or render plain text.", "text": "Q: Vue app uses v-html with forum posts. What is the correct policy? A: Do not v-html untrusted HTML; sanitize with a vetted library or render plain text."}
{"skill": "web_languages_exam", "question": "Bootstrap page looks fine desktop but tiny on phone. Likely missing HTML piece?", "answer": "The viewport meta tag (width=device-width, initial-scale=1).", "text": "Q: Bootstrap page looks fine desktop but tiny on phone. Likely missing HTML piece? A: The viewport meta tag (width=device-width, initial-scale=1)."}
{"skill": "web_languages_exam", "question": "JSON API client uses == null checks inconsistently and treats 0 as missing. Which operator distinguishes nullish only?", "answer": "Use ?? (nullish coalescing) instead of || when 0 or '' are valid values.", "text": "Q: JSON API client uses == null checks inconsistently and treats 0 as missing. Which operator distinguishes nullish only? A: Use ?? (nullish coalescing) instead of || when 0 or '' are valid values."}
{"skill": "web_languages_exam", "question": "Explain one concrete reason to prefer HttpOnly cookies over localStorage for session ids.", "answer": "JavaScript cannot read HttpOnly cookies, reducing XSS exfiltration of the session secret.", "text": "Q: Explain one concrete reason to prefer HttpOnly cookies over localStorage for session ids. A: JavaScript cannot read HttpOnly cookies, reducing XSS exfiltration of the session secret."}
{"skill": "web_languages_exam", "question": "CSS specificity fight: #nav a vs .nav-link. Which wins if both apply, all else equal?", "answer": "#nav a wins because an id selector outranks a class.", "text": "Q: CSS specificity fight: #nav a vs .nav-link. Which wins if both apply, all else equal? A: #nav a wins because an id selector outranks a class."}
{"skill": "web_languages_exam", "question": "async Task in C# vs sync blocking on a web thread — why prefer await for IO?", "answer": "Await frees the thread while waiting on IO instead of blocking it for the whole operation.", "text": "Q: async Task in C# vs sync blocking on a web thread — why prefer await for IO? A: Await frees the thread while waiting on IO instead of blocking it for the whole operation."}
{"skill": "web_languages_exam", "question": "XML parser with default entity expansion enabled parses uploads. Name the class of issue and a mitigation.", "answer": "XXE; disable external entity resolution / use a safe parser configuration.", "text": "Q: XML parser with default entity expansion enabled parses uploads. Name the class of issue and a mitigation. A: XXE; disable external entity resolution / use a safe parser configuration."}
{"skill": "web_languages_exam", "question": "Justify content vs align-items in a row flex container?", "answer": "justify-content along the main axis (horizontal); align-items along the cross axis (vertical).", "text": "Q: Justify content vs align-items in a row flex container? A: justify-content along the main axis (horizontal); align-items along the cross axis (vertical)."}
{"skill": "web_languages_exam", "question": "A SPA starts many searches while typing without cancellation. What API prevents racey late responses?", "answer": "AbortController signals to abort prior fetch calls when a new keystroke starts.", "text": "Q: A SPA starts many searches while typing without cancellation. What API prevents racey late responses? A: AbortController signals to abort prior fetch calls when a new keystroke starts."}
{"skill": "web_languages_exam", "question": "Java JDBC Statement with string concatenation vs PreparedStatement — pick one and why for user input.", "answer": "PreparedStatement with bind parameters prevents SQL injection from input concatenation.", "text": "Q: Java JDBC Statement with string concatenation vs PreparedStatement — pick one and why for user input. A: PreparedStatement with bind parameters prevents SQL injection from input concatenation."}
{"skill": "web_languages_exam", "question": "Describe a safe alt text policy for a purely decorative divider image.", "answer": "Use alt=\"\" so assistive technologies can ignore it.", "text": "Q: Describe a safe alt text policy for a purely decorative divider image. A: Use alt=\"\" so assistive technologies can ignore it."}
{"skill": "web_languages_exam", "question": "Why might min-width:0 on a flex child fix text overflow ellipsis?", "answer": "Flex items default min-width:auto which can prevent shrinking below content size; 0 allows shrinking.", "text": "Q: Why might min-width:0 on a flex child fix text overflow ellipsis? A: Flex items default min-width:auto which can prevent shrinking below content size; 0 allows shrinking."}
{"skill": "web_languages_exam", "question": "ES module script vs classic script regarding strict mode and defer defaults?", "answer": "Modules are always strict and deferred by default; classic scripts are not.", "text": "Q: ES module script vs classic script regarding strict mode and defer defaults? A: Modules are always strict and deferred by default; classic scripts are not."}
{"skill": "web_languages_exam", "question": "LEFT JOIN users to orders then COUNT(orders.id) GROUP BY user — why COUNT on the right key?", "answer": "Counting a right-table column ignores NULLs so users with no orders count as zero, not one.", "text": "Q: LEFT JOIN users to orders then COUNT(orders.id) GROUP BY user — why COUNT on the right key? A: Counting a right-table column ignores NULLs so users with no orders count as zero, not one."}
{"skill": "web_languages_exam", "question": "React effect fetches on mount but setState after unmount warns. What cleanup pattern helps?", "answer": "Abort the fetch or set an ignored flag in the effect cleanup before calling setState.", "text": "Q: React effect fetches on mount but setState after unmount warns. What cleanup pattern helps? A: Abort the fetch or set an ignored flag in the effect cleanup before calling setState."}
{"skill": "web_languages_exam", "question": "Bootstrap .col-md-6 with no .row parent — what layout problem occurs?", "answer": "Negative row margins/column padding alignment breaks; columns should sit in a .row inside a container.", "text": "Q: Bootstrap .col-md-6 with no .row parent — what layout problem occurs? A: Negative row margins/column padding alignment breaks; columns should sit in a .row inside a container."}
